<p id="notice"><%= notice %></p>

<div class="card-group">
  <div class="row row-cols-1 row-cols-md-3 g-4">
  <% @items.each do |item| %>
  <div class="col">
    <div class="card border-dark text-white bg-dark mb-3 h-100" style="width: 18rem;">
      <%= image_tag("#{item.image_url}", class: "card-img-top mw-100 mh-100", style: "width: 350px; height: 275px;") %>
      <div class="card-body">
          <h5 class="card-title"><%= item.name %></h5>
          <h6 class="card-subtitle mb-2 text-muted">$ <%= item.price %></h6>
          <p class="card-text"><%= item.description %></p>
          <%= link_to 'Show', item, class: "card-link" %>
          <%= link_to 'Edit', edit_item_path(item), class: "card-link" %>
          <%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' }, class: "card-link" %>
      </div>
    </div>
  </div>
    <% end %>
  </div>
</div>


<br>

<%= link_to 'New Item', new_item_path %>
